spring:
  main:
    allow-circular-references: true
  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true
    open-in-view: false
  datasource:
      url: jdbc:postgresql://localhost:5432/shop
      username: postgres
      password: Sensey1337
